package drawing;

/**
 * The CommandListener interface defines a method to handle drawing commands
 * when they are released.
 */
public interface CommandListener {

    void onReleaseCommand(String command);
}
     
